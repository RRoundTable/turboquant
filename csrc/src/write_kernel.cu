// CUDA write kernel launcher — thin wrapper around the .cuh header.
// Compiled as .cu to enable CUDA syntax.

#include "turboquant/write_kernel.cuh"

// The kernel and launcher are fully defined in the header.
// This file exists to provide a .cu compilation unit.
